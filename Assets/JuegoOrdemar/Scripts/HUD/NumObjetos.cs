using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class NumObjetos : MonoBehaviour
{
    /*[SerializeField] TextMeshProUGUI NumeroCocina;
    [SerializeField] TextMeshProUGUI NumeroDormitorio;
    [SerializeField] TextMeshProUGUI NumeroJuguetes;
    [SerializeField] TextMeshProUGUI NumeroBaño;
    [SerializeField] string CocinaAuxiliar;
    [SerializeField] string DormitorioAuxiliar;
    [SerializeField] string JuguetesAuxiliar;
    [SerializeField] string BañoAuxiliar;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        CocinaAuxiliar = gameManager.Instance.ObjetosCocina.ToString();
        NumeroCocina.text = "Cocina: " + CocinaAuxiliar;
        DormitorioAuxiliar = gameManager.Instance.ObjetosDormitorio.ToString();
        NumeroDormitorio.text = "Dormitorio: " + DormitorioAuxiliar;
        JuguetesAuxiliar = gameManager.Instance.ObjetosJuguetes.ToString();
        NumeroJuguetes.text = "Juego: " + JuguetesAuxiliar;
        BañoAuxiliar = gameManager.Instance.ObjetosBaño.ToString();
        NumeroBaño.text = "Baño: " + BañoAuxiliar;
    }*/
}
