using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class HabitacionSolicitada : MonoBehaviour
{
    //[SerializeField] TextMeshProUGUI Solicitada;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //Solicitada.text = gameManager.Instance.HabitacionPedida.ToString();
    }
}
