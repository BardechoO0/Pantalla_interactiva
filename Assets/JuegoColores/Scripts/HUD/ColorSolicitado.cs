using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class ColorSolicitado : MonoBehaviour
{

    //[SerializeField] public TextMeshProUGUI Solicitado;
    // Start is called before the first frame update
    void Start()
    {
        
    }
    
    // Update is called once per frame
    void Update()
    {
        //Solicitado.text = gameManager.Instance.ColorPedido.ToString();
    }
}